# Optional local overrides, loaded by `make start`. Copy to .env (git-ignored).
# Development needs none of these; they exist for testing real services locally.

# MAIL_ADAPTER=resend
# RESEND_API_KEY=re_...
# MAIL_FROM=hello@example.com
